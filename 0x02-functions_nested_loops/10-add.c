#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @c: first number to be computed
 * @i: second number to be computed
 * Return: sum of a and b
 */
int add(int c, int i)
{
	int x;

	x = c + i;
	return (x);
}
